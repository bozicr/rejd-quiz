import React from 'react';

/**
 * UI for ErrorBoundary component. It represents fallback
 */
function ErrorFallback() {
  return (
    <div style={{ color: 'white', fontSize: '30px', textAlign: 'center' }}>Error happened</div>
  );
}

export default ErrorFallback;
